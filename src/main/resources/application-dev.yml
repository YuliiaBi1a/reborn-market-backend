spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/airlines_data_base
    username: yuliia
    password: yuliia
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: NEVER